#include <stdio.h>

int main(int argc, char* argv[argc]) {
    int a;
    int b;
    printf("Type a latter:");
    a = getchar();

    printf("Type a letter:");
    b = getchar();

    printf("a='%c', b='%c'\n",a ,b);

    return 0;
}
